const el=document.querySelector('#albums'),modal=document.querySelector('#modal');async function check(){const r=await fetch('/api/admin/me');if(!(await r.json()).authenticated)location.href='/admin/login'}async function load(){const r=await fetch('/api/albums');const a=await r.json();el.innerHTML=a.map(x=>`<div class="admin-card">${x.cover?`<img src="${x.cover}" alt="">`:''}<div class="body"><h3>${x.title}</h3><p>${x.photoCount} foto${x.photoCount==1?'':'s'}</p><a href="/admin/album?id=${x.id}">Gerenciar →</a></div></div>`).join('')||'<p>Nenhum álbum ainda.</p>'}check().then(load);document.querySelector('#new').onclick=()=>modal.classList.add('show');document.querySelector('#cancel').onclick=()=>modal.classList.remove('show');document.querySelector('#logout').onclick=async()=>{await fetch('/api/admin/logout',{method:'POST'});location.href='/admin/login'};document.querySelector('#albumForm').onsubmit=async e=>{e.preventDefault();const r=await fetch('/api/admin/albums',{method:'POST',body:new FormData(e.target)});if(r.ok){modal.classList.remove('show');e.target.reset();load()}else alert((await r.json()).error)};
