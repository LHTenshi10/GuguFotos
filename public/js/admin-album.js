const id=new URLSearchParams(location.search).get('id'), photosEl=document.querySelector('#photos');async function check(){const r=await fetch('/api/admin/me');if(!(await r.json()).authenticated)location.href='/admin/login'}async function load(){const r=await fetch('/api/albums/'+id);if(!r.ok){location.href='/admin';return}const a=await r.json();document.querySelector('#title').textContent=a.title;document.querySelector('#description').textContent=a.description||'';photosEl.innerHTML=a.photos.map(p=>`<div class="photo-admin"><img src="${p.url}" alt=""><button data-id="${p.id}">×</button></div>`).join('')||'<p>Nenhuma foto ainda.</p>';photosEl.querySelectorAll('button').forEach(b=>b.onclick=async()=>{if(confirm('Excluir esta foto?')){await fetch('/api/admin/photos/'+b.dataset.id,{method:'DELETE'});load()}})}check().then(load);document.querySelector('#uploadForm').onsubmit=async e=>{e.preventDefault();const r=await fetch('/api/admin/albums/'+id+'/photos',{method:'POST',body:new FormData(e.target)});if(r.ok){e.target.reset();load()}else alert((await r.json()).error)};document.querySelector('#deleteAlbum').onclick=async()=>{if(confirm('Excluir álbum e todas as fotos?')){await fetch('/api/admin/albums/'+id,{method:'DELETE'});location.href='/admin'}};
