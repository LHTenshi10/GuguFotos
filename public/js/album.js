const id=new URLSearchParams(location.search).get('id'), photosEl=document.querySelector('#photos'), lb=document.querySelector('#lightbox'), img=document.querySelector('#lightboxImg');let photos=[],idx=0;
(async()=>{const r=await fetch('/api/albums/'+id);if(!r.ok){document.querySelector('#title').textContent='Álbum não encontrado';return}const a=await r.json();document.title=a.title+' — GuguFotos';document.querySelector('#title').textContent=a.title;document.querySelector('#description').textContent=a.description||'';document.querySelector('#count').textContent=a.photos.length+' FOTO'+(a.photos.length===1?'':'S');photos=a.photos;photosEl.innerHTML=photos.map((p,i)=>`<img src="${p.url}" alt="${p.originalName}" data-i="${i}">`).join('');photosEl.querySelectorAll('img').forEach(x=>x.onclick=()=>open(+x.dataset.i));})();
function open(i){idx=i;img.src=photos[idx].url;lb.classList.add('open')}function close(){lb.classList.remove('open')}function move(n){idx=(idx+n+photos.length)%photos.length;img.src=photos[idx].url}document.querySelector('#close').onclick=close;document.querySelector('#prev').onclick=()=>move(-1);document.querySelector('#next').onclick=()=>move(1);lb.onclick=e=>{if(e.target===lb)close()};document.onkeydown=e=>{if(e.key==='Escape')close();if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)};
